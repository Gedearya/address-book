{
  "name": "contact-api-testing",
  "version": "1.0.0",
  "description": "Unit testing for Contact API CRUD operations",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ],
    "collectCoverageFrom": [
      "index.js"
    ]
  }
}
